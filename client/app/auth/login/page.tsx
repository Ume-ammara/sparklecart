import LoginForm from "@/components/auth_comps/LoginForm";

const page = () => {
  return (
    <div className="bg-background w-full min-h-screen flex items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default page;
