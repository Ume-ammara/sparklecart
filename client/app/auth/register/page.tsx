import RegisterForm from "@/components/auth_comps/RegisterForm";

const page = () => {
  return (
    <div className="w-full min-h-screen flex items-center justify-center">
      <RegisterForm />
    </div>
  );
};

export default page;
